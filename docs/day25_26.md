CREATED PROGRAMS FOR DETECTING PARTITIONS IN A DISK
    
WORKDONE:
    created a partition scanner that can scan for fat12,fat16,linux file system and make it to a linked list like structure and be stored as a value at end of the disk structure, and the last partition will be null terminated

NOTES:
    
TOOLS:
    geany, qemu, 
