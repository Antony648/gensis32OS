#include "hash.h"
#include <stdint.h>
uint64_t generate_hash(char* path)
{
    //destroy the last / donot include in path, and exclude null char at string termination
    return 0;
}
uint64_t generate_hast_start_end(char *str, uint32_t start, uint32_t end)
{
    //inclusive ; include start and end
    return 0;
}
