Day4- CREATE MAKEFILE,BASH_SCRIPT AND LINKER SCRIPT

WORKDONE:
    created a folder called final for storing os.bin
    created linker file for for kernel.bin file 
    created makefile for 
    split the 32 bit kernel part from bootloader and created as a separate kernel.bin file
    analysed the final os.bin
NOTES:
    [explanation and neccesity of linker script](../notes/linker_notes)
    [indepth explanation of Makefile](../notes/make_file)
    [explanation of bash script](../notes/bash_script_exp)

TOOLS:
    GNU make, vim, hexdump 
