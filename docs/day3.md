Day3- CREATE CROSSCOMPILER SETUP FOR i686 

WORKDONE:
    installed binutils-2.35 
    generated makefies configured to i686
    installed the toolchains to ~/opt/cross with sysroot support

    installed gcc-10.2.0
    generated makefiles configured to i686 for gcc and libgcc
    installed the gcc toolchain to ~/opt/cross
    
    verified installation of gcc and binutils toolchains

NOTES:
    [notes on installation process debiab](../notes/cross_cmp_debian)
    [notes on installation process archlinux](../notes/cross_cmp_arch)

TOOLS:
    bash shell
